module.exports = {
    /**
     * 计算人物等级
     * @param {*} req 
     * @param {*} res 
     * @param {*} level 等级
     * 
     */
    pushTask: function (req, res, level) {
        switch (level) {
            case 10:
                // 推送一转任务
                return;
            case 30:
                // 推送二转任务
                return;
            case 50:
                // 推送三转任务
                return;
            case 70:
                // 推送四转任务
                return;
            case 80:
                // 推送五转任务
                return;
            case 90:
                // 推送六转任务
                return;
            case 99:
                // 推送七转任务
                return;
        }

    }
};

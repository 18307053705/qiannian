const express = require("express");
const router = new express.Router();

// type
router.post("/getTaskDetail", (req, res) => {
    const { id } = req;
    //   dirFn.rControl(req, res);

});



module.exports = router;
